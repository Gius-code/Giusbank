<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="UTF-8" />
        <!-- logo nella scheda del browser -->
        <link rel="icon" href="/img/logo3.png" type="image/png" />
    <title>Modifica Dati</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/Stile.css" />
  </head>
  <body class="bg-light">
    <%- include('navbar') %>
    <div class="container mt-4">
      <div class="row justify-content-center">
        <!-- La colonna limita la larghezza della card in base alla grandezza dello schermo -->
        <div class="col-12 col-md-8 col-lg-6">
          <!-- card per modificare i dati successivi input e bottoni per passarli al server
           input già compilati col valore attuale  che l'utente può modificare-->
          <div class="card">
            <div class="card-header">
              <h1 class="h4 mb-0">Modifica Dati Utente</h1>
            </div>
            <div class="card-body">
              <div class="mb-3">
                <label for="professione" class="form-label">Professione</label>
                <input
                  type="text"
                  class="form-control"
                  id="professione"
                  value="<%= user.professione %>"
                />
              </div>

              <div class="mb-3">
                <label for="indirizzo" class="form-label">Indirizzo</label>
                <input
                  type="text"
                  class="form-control"
                  id="indirizzo"
                  value="<%= user.indirizzo %>"
                />
              </div>

              <button id="btnSalva" class="btn btn-primary">Salva</button>
              <a href="/api/dashboard" class="btn btn-secondary">Annulla</a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer') %>
    <script>
      // Prendiamo l'id utente dal server
      const userId = "<%= user.id %>";
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/modificaUtente.js"></script>
  </body>
</html>
