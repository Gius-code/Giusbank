<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="UTF-8" />
    <!-- logo e titolo nella scheda del browser -->
    <link rel="icon" href="/img/logo3.png" type="image/png" />
    <title>Cambia Password</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/Stile.css" />
  </head>
  <body class="bg-light">
    <%- include('navbar') %>
    <div class="container mt-4">
      <div class="row justify-content-center">
        <!-- Colonna che limita la larghezza della card -->
        <div class="col-12 col-md-8 col-lg-6">
          <div class="card">
            <div class="card-header">
              <h1 class="h4 mb-0">Cambia Password</h1>
            </div>
            <div class="card-body">
              <p class="mb-3">
                <strong>
                  La password deve avere almeno 8 caratteri di cui almeno uno
                  maiuscolo, uno minuscolo, un numero e un carattere speciale
                </strong>
              </p>
              <div class="mb-3">
                <label for="vecchiaPassword" class="form-label">Vecchia Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="vecchiaPassword"
                  required
                />
              </div>
              <div class="mb-3">
                <label for="nuovaPassword" class="form-label">Nuova Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="nuovaPassword"
                  required
                />
              </div>
              <div class="mb-3">
                <label for="confermaNuovaPassword" class="form-label">Conferma Nuova Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="confermaNuovaPassword"
                  required
                />
              </div>
              <button id="btnCambiaPassword" class="btn btn-primary">Conferma</button>
              <a href="/api/dashboard" class="btn btn-secondary">Annulla</a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // Trasferimento dell'ID utente dal server 
      const userId = "<%= user.id %>";
    </script>
    <script src="/cambiaPassword.js"></script>
  </body>
</html>
